version = 1
id = "request"
kind = "cron"
name = "REQUEST 13H"
prompt = "Traitement des requests - repo JOURNAUX (GitHub-first)\n\nSurveille uniquement le repo `C:\\Users\\engue\\__SYSTEME - GITHUB__\\JOURNAUX` pour detecter la presence de fichiers `REQUEST__*.md`.\n\nRegles de cadrage :\n- ne pas scanner d autres repos pour la detection initiale\n- ne pas utiliser de helper PowerShell complexe\n- ne pas manipuler a la fois `http_proxy` et `HTTP_PROXY` comme variables distinctes: PowerShell est insensible a la casse\n- ne pas transformer les lignes de progression Git sur stderr en erreur si le code de sortie est `0`\n- s appuyer sur les commandes Git directes et leur code de sortie\n\nSource de verite pour la detection :\n- `origin/main` du repo `JOURNAUX`\n- ne pas se limiter a l etat local non synchronise\n\nPre-controle obligatoire :\n1. Lire la memoire de l automation si elle existe.\n2. Travailler uniquement dans `C:\\Users\\engue\\__SYSTEME - GITHUB__\\JOURNAUX`.\n3. Verifier que le dossier est bien un depot Git valide.\n4. Verifier que la branche courante est `main`; sinon STOP avec `vigilance`.\n5. Executer `git -c http.proxy= -c https.proxy= fetch origin --prune`.\n6. Detecter les `REQUEST__*.md` sur `origin/main`.\n7. Si aucune request distante n est detectee : STOP (ne rien faire).\n\nRegle de priorite :\n1. S'il n'y a aucune request, ne rien faire.\n2. S'il y a une request, la lire et la prendre en charge.\n3. Identifier le perimetre et le repo cible a partir de la request.\n4. Ouvrir ou utiliser la branche de travail correspondante dans le repo cible.\n5. Executer la request sur cette branche.\n6. Mettre a jour le journal lie pour noter :\n   - que la request a ete prise en charge,\n   - quelle branche a ete ouverte ou reprise,\n   - ce qui a ete fait,\n   - ce qui bloque eventuellement,\n   - l'etat courant.\n7. Si la request s'appuie sur un suivi, mettre a jour le suivi concerne.\n8. Supprimer la request seulement si elle a ete correctement prise en charge.\n9. Si la request n'a pas pu etre correctement prise en charge, ne pas la supprimer et noter le blocage dans le journal.\n\nCriteres minimum de prise en charge correcte :\n- branche ouverte ou reprise dans le repo cible\n- execution effectivement engagee\n- journal canonique mis a jour\n\nRegles STOP :\n- aucune request detectee sur GitHub distant\n- branche courante de `JOURNAUX` differente de `main`\n- perimetre ambigu\n- repo cible ambigu\n- request incomplete ou contradictoire\n- action destructive non validee\n- echec `fetch` avec code de sortie non nul\n\nImportant :\n- ne pas traiter directement les journaux, suivis ou notes hors request\n- ne pas fermer automatiquement la branche\n- ne pas merger automatiquement sauf si une regle explicite du projet l'autorise\n- privilegier une lecture incrementale et l'absence d'actions inutiles\n- en cas d'echec de prise en charge, conserver la request et consigner le blocage\n- mettre a jour `memory.md` avec un resume tres court du run et le resultat final"
status = "ACTIVE"
rrule = "RRULE:FREQ=WEEKLY;BYHOUR=13;BYMINUTE=0;BYDAY=SU,MO,TU,WE,TH,FR,SA"
model = "gpt-5.3-codex"
reasoning_effort = "medium"
execution_environment = "local"
cwds = ["C:\\Users\\engue\\__SYSTEME - GITHUB__\\JOURNAUX"]
created_at = 1776684364126
updated_at = 1776726903689

